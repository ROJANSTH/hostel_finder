import DashboardShell from "@/components/DashboardShell";import{authRequest}from"@/lib/api/client";import OwnerBookingStatus from"./OwnerBookingStatus";interface Row{_id:string;user?:{name:string;email:string};hostel?:{name:string;city:string};checkIn:string;checkOut:string;guests:number;totalPrice:number;status:string;paymentMethod:string;paymentStatus:string}export default async function OwnerBookings(){const r=await authRequest<Row[]>("/owner/hostels/bookings");const rows=r.success?r.data:[];return <DashboardShell><h1 className="text-3xl font-bold">Property bookings</h1><div className="mt-6 overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-zinc-50"><tr>{["Guest","Property","Dates","Payment","Total","Status"].map(h=><th key={h} className="p-4">{h}</th>)}</tr></thead><tbody>{rows.map(b=><tr key={b._id} className="border-t"><td className="p-4"><strong>{b.user?.name}</strong><br/><span className="text-zinc-500">{b.user?.email}</span></td><td className="p-4">{b.hostel?.name}</td><td className="p-4">{new Date(b.checkIn).toLocaleDateString()} – {new Date(b.checkOut).toLocaleDateString()}</td><td className="p-4 capitalize">{b.paymentMethod} · {b.paymentStatus}</td><td className="p-4">NPR {b.totalPrice.toLocaleString()}</td><td className="p-4"><OwnerBookingStatus id={b._id} status={b.status} paymentStatus={b.paymentStatus}/></td></tr>)}</tbody></table>{!rows.length&&<p className="p-10 text-center text-zinc-500">No property bookings yet.</p>}</div></DashboardShell>}
