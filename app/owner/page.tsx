import Link from "next/link"; import DashboardShell from "@/components/DashboardShell"; import { authRequest } from "@/lib/api/client"; import { Hostel } from "@/lib/types/hostel.types"; import OwnerPropertyClient from "./OwnerPropertyClient";
interface Stats{hostels:number;bookings:number;activeBookings:number;revenue:number}
export default async function OwnerPage(){const[stats,properties]=await Promise.all([authRequest<Stats>("/owner/hostels/stats"),authRequest<Hostel[]>("/owner/hostels")]);const s=stats.success?stats.data:{hostels:0,bookings:0,activeBookings:0,revenue:0};return <DashboardShell><div className="flex justify-between"><div><p className="text-sm font-bold uppercase tracking-widest text-indigo-600">Hostel owner</p><h1 className="text-3xl font-bold">Property dashboard</h1></div><Link href="/owner/bookings" className="rounded-xl bg-zinc-900 px-5 py-3 text-sm font-bold text-white">Manage bookings</Link></div><div className="mt-6 grid gap-4 sm:grid-cols-4">{[["Properties",s.hostels],["All bookings",s.bookings],["Active stays",s.activeBookings],["Revenue",`NPR ${s.revenue.toLocaleString()}`]].map(([l,v])=><div key={l} className="rounded-2xl border bg-white p-5"><p className="text-sm text-zinc-500">{l}</p><p className="mt-2 text-2xl font-bold">{v}</p></div>)}</div><OwnerPropertyClient hostels={properties.success?properties.data:[]}/></DashboardShell>}
