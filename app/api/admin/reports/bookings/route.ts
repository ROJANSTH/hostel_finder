import { getAuthToken } from "@/lib/api/client";
export async function GET(){const token=await getAuthToken();if(!token)return new Response("Unauthorized",{status:401});const api=process.env.NEXT_PUBLIC_API_URL||"http://localhost:5000/api/v1";const response=await fetch(`${api}/admin/hostels/reports/bookings.csv`,{headers:{Authorization:`Bearer ${token}`},cache:"no-store"});return new Response(response.body,{status:response.status,headers:{"Content-Type":response.headers.get("Content-Type")||"text/csv","Content-Disposition":"attachment; filename=hostel-bookings.csv"}})}
