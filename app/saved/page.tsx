import DashboardShell from "@/components/DashboardShell"; import HostelCard from "@/components/HostelCard"; import { getSaved } from "@/lib/api/hostel.api";
export default async function SavedPage(){ const r=await getSaved(); const rows=r.success?r.data:[]; return <DashboardShell><h1 className="text-3xl font-bold">Saved hostels</h1><p className="mt-2 text-zinc-500">Your shortlist for later. Use the Saved button to remove a hostel.</p><div className="mt-6 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{rows.map(h=><HostelCard key={h._id} hostel={h} initiallySaved/>)}</div>{!rows.length&&<p className="mt-8 rounded-2xl border border-dashed p-10 text-center text-zinc-500">You have not saved any hostels yet.</p>}</DashboardShell>}
